apiVersion: v1
kind: Service
metadata:
  name: lab8-service
spec:
  type: NodePort
  selector:
    app: lab8
  ports:
    - name: app1-port
      port: 80
      targetPort: 80
      nodePort: 30007

    - name: app2-port
      port: 8080
      targetPort: 8080
      nodePort: 30008